#page-links-year.page
  %header.page-header
    %h1= t('.title', year: @year)
  %ul
    - @links.pluck(:published_at).map(&:month).uniq.each do |month|
      %li= link_to(Date::MONTHNAMES[month], "#{links_path}/#{@year}/#{month}")
